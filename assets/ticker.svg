<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 581 87" width="581" height="87" role="img" aria-labelledby="t d" xml:space="preserve">
<title id="t">Recent commits</title>
<desc id="d">A scrolling ticker of the most recent commit subjects across the public repositories, newest first.</desc>
<style>
.glass{fill:#15110C}
.edge{fill:none;stroke:#241B12;stroke-width:1}
text{font-family:ui-monospace,SFMono-Regular,"SF Mono",Menlo,Consolas,"Cascadia Mono","DejaVu Sans Mono","Liberation Mono",monospace;font-size:14px;white-space:pre;fill:#E8A33D}
.fr{fill:#B07D2A} .hi{fill:#F7E7C6} .ac{fill:#9B3A2E} .dm{fill:#8F6C38}
.cool{fill:#E8E4D2} .deep{fill:#7A2620} .body{fill:#E8A33D} .scrim{opacity:1}
.rust{fill:#C25A1E} .gold{fill:#D9B45B} .plum{fill:#8E4A5E}
.g0{fill:#5A3A18} .g1{fill:#8A5A20} .g2{fill:#C25A1E} .g3{fill:#E8A33D}
.q0{fill:#3A2E1E} .q1{fill:#8A6224} .q2{fill:#C08A2E} .q3{fill:#E8A33D} .q4{fill:#F7E7C6}
.scan{opacity:.05}
.vig{opacity:1}
@media (prefers-color-scheme: light){
  .glass{fill:#F2E7D3}
  .edge{stroke:#DCC9A8}
  text{fill:#8A4F14}
  .fr{fill:#9A6E24} .hi{fill:#2B1D0E} .ac{fill:#8C2F27} .dm{fill:#8A7050}
  .cool{fill:#3F4A44} .deep{fill:#6B1F1A} .body{fill:#8A4F14} .scrim{opacity:0}
  .rust{fill:#A8430F} .gold{fill:#8A6A1E} .plum{fill:#7A3A4C}
  .g0{fill:#D8C4A2} .g1{fill:#BE9354} .g2{fill:#A8430F} .g3{fill:#8A4F14}
  .q0{fill:#DFCDAC} .q1{fill:#C9A263} .q2{fill:#A9741F} .q3{fill:#8A4F14} .q4{fill:#4A2A0A}
  .scan{opacity:.028}
  .vig{opacity:.18}
}

/* One of only two continuously-animating elements on the whole profile. The
   strip is rendered twice back to back and translated by exactly one copy's
   width, so the loop has no visible seam. textLength pins that width to the
   grid, which is what keeps the seam invisible on a font whose advance is
   not the CH this file assumes. */
@keyframes roll{from{transform:translateX(0)}to{transform:translateX(-14086.8px)}}
.mv{animation:roll 209s linear infinite}
@media (prefers-reduced-motion: reduce){ .mv{animation:none} }
</style>
<defs>
  <pattern id="sl" width="6" height="6" patternUnits="userSpaceOnUse">
    <rect width="6" height="3" fill="#000"/>
  </pattern>
  <radialGradient id="vig" cx="50%" cy="46%" r="78%">
    <stop offset="58%" stop-color="#000" stop-opacity="0"/>
    <stop offset="100%" stop-color="#000" stop-opacity=".34"/>
  </radialGradient>
</defs>
<rect class="glass" width="581" height="87" rx="8"/>
<text x="22" y="43" textLength="537.6" lengthAdjust="spacing"><tspan class="fr">┌─ RECENT COMMITS ────────────────────────────── newest first ─┐</tspan></text>
<clipPath id="cl"><rect x="38.8" y="47" width="504.0" height="23"/></clipPath>
<text x="22" y="60" textLength="537.6" lengthAdjust="spacing"><tspan class="fr">│                                                              </tspan><tspan class="fr">│</tspan></text>
<g clip-path="url(#cl)"><g class="mv"><text class="dm" x="38.8" y="60" textLength="28173.6" lengthAdjust="spacing">  ulpf · license: the MIT file the workspace manifest has always declared   ◦  ulpf · records: CI run 4 read, the measurement job corrected, live session #2   ◦  ulpf · ci: the Windows driver judges a key by whether the UI routes it, not…   ◦  ulpf · ui: an unsaved edit blocks approve, not only regenerate   ◦  ulpf · tests: the failed-parse allocation check measures the deficit it clai…   ◦  ulpf · parse: the xml document starts at the first '&lt;', so a forwarder's tag…   ◦  ulpf · docs: the mappings count, the samples exclusion, and the Windows prov…   ◦  ulpf · docs: the README counter block is re-pasted from the dist binary   ◦  ulpf · records: the v5 closing checkpoint, the review pass, and CI run 3   ◦  ulpf · ci: the resume test drains watch/ before the force kill   ◦  ulpf · ci: oskeys.ps1 injects keys with keybd_event, not SendKeys   ◦  ulpf · ui: regenerate refuses to discard an unsaved edit   ◦  flint · ci(release): make bundle-rename step bash 3.2 compatible (macOS)   ◦  flint · ci(release): make macOS signing truly inert without secrets   ◦  flint · chore(release): v0.1.4 — hardening + macOS install   ◦  flint · docs(macos): add INSTALL_MACOS.md + README quarantine fix   ◦  flint · ci: inert macOS sign/notarize pipeline + rename guard + install notes…   ◦  flint · macos: ad-hoc sign the bundle by default (signingIdentity &quot;-&quot;)   ◦  flint · docs: correct doc-vs-code drift in README and CLAUDE.md   ◦  flint · docs(plugins): make manifest events arrays match actual subscriptions   ◦  flint · fix(plugins): countdown finalize-before-notify + diagnostic pomodoro…   ◦  flint · fix(plugin-settings): per-key pending state for concurrent saves   ◦     ulpf · license: the MIT file the workspace manifest has always declared   ◦  ulpf · records: CI run 4 read, the measurement job corrected, live session #2   ◦  ulpf · ci: the Windows driver judges a key by whether the UI routes it, not…   ◦  ulpf · ui: an unsaved edit blocks approve, not only regenerate   ◦  ulpf · tests: the failed-parse allocation check measures the deficit it clai…   ◦  ulpf · parse: the xml document starts at the first '&lt;', so a forwarder's tag…   ◦  ulpf · docs: the mappings count, the samples exclusion, and the Windows prov…   ◦  ulpf · docs: the README counter block is re-pasted from the dist binary   ◦  ulpf · records: the v5 closing checkpoint, the review pass, and CI run 3   ◦  ulpf · ci: the resume test drains watch/ before the force kill   ◦  ulpf · ci: oskeys.ps1 injects keys with keybd_event, not SendKeys   ◦  ulpf · ui: regenerate refuses to discard an unsaved edit   ◦  flint · ci(release): make bundle-rename step bash 3.2 compatible (macOS)   ◦  flint · ci(release): make macOS signing truly inert without secrets   ◦  flint · chore(release): v0.1.4 — hardening + macOS install   ◦  flint · docs(macos): add INSTALL_MACOS.md + README quarantine fix   ◦  flint · ci: inert macOS sign/notarize pipeline + rename guard + install notes…   ◦  flint · macos: ad-hoc sign the bundle by default (signingIdentity &quot;-&quot;)   ◦  flint · docs: correct doc-vs-code drift in README and CLAUDE.md   ◦  flint · docs(plugins): make manifest events arrays match actual subscriptions   ◦  flint · fix(plugins): countdown finalize-before-notify + diagnostic pomodoro…   ◦  flint · fix(plugin-settings): per-key pending state for concurrent saves   ◦   </text></g></g>
<text x="22" y="77" textLength="537.6" lengthAdjust="spacing"><tspan class="fr">└─────────────────────────── 22 commits across 3 repositories ─┘</tspan></text>
<rect class="scan" x="0" y="-12" width="581" height="111" fill="url(#sl)"/>
<rect class="vig" width="581" height="87" rx="8" fill="url(#vig)"/>
<rect class="edge" x=".5" y=".5" width="580" height="86" rx="8"/>
</svg>
